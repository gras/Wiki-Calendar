function select_name(name) {
    document.getElementById("name_box").value=name;
    }

function select_date(date) {
    document.getElementById("date_box").value=date;
    }

function select_entry(text) {
    document.getElementById("entry_box").value=text;
    }